name: wivoce_laravel_echo_client
description: 基于laravel-echo插件.
version: 1.0.0
homepage: https://github.com/one-flower/wivoce-laravel-echo-client

environment:
  sdk: ">=3.3.4 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
